# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test artifacts
*.test
*.out
coverage.html
coverage.out

# Go workspace
go.work
go.work.sum

# Local config
dark-research.toml
*.local.yaml

# LLM response cache (regenerated on demand)
/llm-cache.json

# Temp / scratch files used by smoke tests
/smoke-cache-*.json
/scratch/

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# dark.db (production SQLite; lives outside the repo by convention)
dark.db
dark.db-

# Build artifacts for releases (uploaded to GitHub Releases instead)
/dist/*

# Local debugging artifacts (not part of the test suite)
debug_test.go
debug_detect_test.go
test_minimal.toml
test_minimal_mod.go

# Local dev scripts / logs (never publish)
*.bak
*.bak-*
mock-llm.*.log
mock-llm.err.log
mock-llm.out.log
oc.bat
opencode-with-vault.*
verify-llm-pipeline.*
dark-agents-harvest-now.ps1
dark-agents-watchdog.ps1
tag-msg-*.txt
report.json

# Build output (make build / make release land here)
/bin/
